<div
	class="hanging-owl pointer-events-none absolute top-0 z-30"
	aria-hidden="true"
	data-hanging-owl
	data-visible="false"
>
	<img
		src="/assets/owl-moon-branch.png"
		class="hanging-owl-art block w-full object-contain"
		width="1254"
		height="1254"
		decoding="async"
	/>
</div>

<style>
	.hanging-owl {
		left: 2%;
		top: 0;
		z-index: 30;
		width: clamp(9rem, 18vw, 16rem);
		opacity: 0;
		translate: 0 -18%;
		filter: drop-shadow(0 0 3.5px rgba(243, 238, 227, 0.98)) drop-shadow(0 6px 8px rgba(4, 10, 24, 0.25));
		transition:
			opacity 420ms ease-out,
			translate 650ms cubic-bezier(0.16, 1, 0.3, 1);
	}

	.hanging-owl[data-visible="true"] {
		opacity: 1;
		translate: 0 0;
	}

	.hanging-owl-art {
		transform-origin: 50% 0;
		animation: hanging-owl-sway 8s ease-in-out infinite alternate;
	}

	@media (max-width: 639px) {
		.hanging-owl {
			left: 1%;
			width: clamp(7rem, 28vw, 10rem);
		}
	}
	@media (min-width: 640px) and (max-width: 1439px) {
		.hanging-owl {
			left: -2%;
			width: clamp(9rem, 15vw, 12rem);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.hanging-owl {
			transition: none;
		}

		.hanging-owl-art {
			animation: none;
		}
	}

	@keyframes hanging-owl-sway {
		from {
			transform: rotate(-1.5deg);
		}
		to {
			transform: rotate(1.5deg);
		}
	}
</style>
